/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store
desktop.ini

# Root /images is a local source dump; not served. public/images IS served
# (secondary pages reference local /images/* assets), so it is committed.
/images
